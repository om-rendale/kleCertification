
let pi = Math.PI

//Arrow function

let sum = ()=>{
    return 2+3;
}

let maxNum = Math.max(2,6,8,1,20);
// module.exports = {pi, sum, maxNum};//exports the value in app.js

// console.log(person);
let person = {
    name : 'om',
    age : 20,
    favColor : 'White',
    course : 'BCA',
}
module.exports = {pi, sum, maxNum, person};